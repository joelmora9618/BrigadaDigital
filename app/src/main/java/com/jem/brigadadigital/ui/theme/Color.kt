package com.jem.brigadadigital.ui.theme

import androidx.compose.ui.graphics.Color

// Reference-Inspired Palette (Deep Mode)
val DeepBackground = Color(0xFF16161D) // Dark Blue-Grey
val SurfaceCard = Color(0xFF21212B)     // Slightly lighter for cards
val SurfaceVariant = Color(0xFF2A2A35)
val BorderLight = Color(0xFF32323D)

// Accents from reference
val AccentOrange = Color(0xFFFD6249) // Coral/Orange from reference
val AccentBlue = Color(0xFF4080FF)   // Blue from reference
val AccentTeal = Color(0xFF40CFCF)   // Teal from reference

// Secondary Accents
val VibrantRed = Color(0xFFFF3B30)
val VividGreen = Color(0xFF32D74B)

// Text
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFF9BA1B0)
val TextMuted = Color(0xFF636D7D)
val TextOnAccent = Color(0xFFFFFFFF)